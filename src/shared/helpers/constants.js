const USER_FIELD='-password -refreshToken -__v -socketId -idAttachment -idName'

const ROLES = {
    ADMIN: 'admin',
    DEFAULT: 'default'
}

const INKLINE_URL = 'http://localhost:8000'

module.exports = {
    USER_FIELD,
    ROLES,
}